import { LoginCheckDirective } from './login-check.directive';

describe('LoginCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginCheckDirective();
    expect(directive).toBeTruthy();
  });
});
